import React from 'react'
import AuthForm from '@/components/AuthForm'
// This page is for user sign-in
// It uses the AuthForm component with type "sign-in"

const page = () => {
  return <AuthForm type="sign-in"/> 
}

export default page